---
category: Forms
---
# Textarea

Multi-line field for notes and long-form facts. Minimum height is 64px and it
grows with its content (`field-sizing-content`) rather than scrolling — so a long
note pushes the form down instead of hiding itself.

Same border, radius and focus ring as `Input`; same `aria-invalid` and `disabled`
treatments.
